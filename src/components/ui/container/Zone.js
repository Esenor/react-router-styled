import styled from 'styled-components'
import { backgroundColors } from '../../../app/skin'

const Zone = styled.div`
  background: ${backgroundColors.primary};
  padding: 20px;
  margin-bottom: 30px;
  border-radius: 2px;
`

export default Zone
